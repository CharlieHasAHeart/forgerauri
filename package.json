{
  "name": "forgetauri",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "description": "ForgeTauri architecture repository (core/shell/profile/protocol)",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run --passWithNoTests",
    "test:unit": "vitest run --passWithNoTests",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@types/node": "^25.3.0",
    "typescript": "^5.9.3",
    "vitest": "^4.0.18"
  }
}
