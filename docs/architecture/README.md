# Architecture

This repository uses a core-first architecture:

- `src/core/**` is the runtime engine and contract surface.
- `src/profiles/**` is the external assembler layer.

For canonical terms, see `docs/terminology.md`.

For naming and placement rules, see `docs/architecture/naming-conventions.md`.
